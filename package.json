{
  "name": "tint-website",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "smoke": "tsx scripts/smoke-copy-buttons.ts && tsx scripts/smoke-demo-videos.ts && tsx scripts/smoke-dist.ts && tsx scripts/smoke-worker.ts",
    "check": "npm run typecheck && npm run lint && npm run build && npm run smoke",
    "preview": "astro preview",
    "astro": "astro",
    "typecheck": "astro check",
    "lint": "biome check",
    "format": "biome format --write",
    "fix": "biome check --write"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.2.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@biomejs/biome": "^2.4.14",
    "jsonc-parser": "^3.3.1",
    "prettier": "^3.4.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "wrangler": "4.87.0"
  }
}
